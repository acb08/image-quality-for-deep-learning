---

description: running blur scan on fr model trained 50 epochs (rather than original 5)

test_dataset_id: 0047tst-coco_n_scan_v2-noise
test_dataset_artifact_alias: latest

model_artifact_id: 0060yolo-v8l-fr-ext_best_loss:v0
model_artifact_alias: latest

batch_size: 8
num_workers: 0
pin_memory: True

status_interval: 500  # images

device: 1

#rgb_flag: False
#dataset_split_key: test  # used to get to correct level of dataset dict (to mirror 'train'/'val' splits)
#last_distortion_type_flag: noise  # normally should be 'noise'
#loss_func: CrossEntropyLoss
